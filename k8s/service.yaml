apiVersion: v1
kind: Service
metadata:
  name: sigma-backend-service
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-type: nlb
    service.beta.kubernetes.io/aws-load-balancer-scheme: internet-facing
    service.beta.kubernetes.io/aws-load-balancer-subnets: subnet-0811da083016b2867,subnet-0a140ec48b1d178a8
spec:
  selector:
    app: sigma-backend
  ports:
    - port: 80
      targetPort: 8080
  type: LoadBalancer
